# Fallback price state if Edge Config is NOT connected.
# Edge Config (set in Vercel dashboard) always wins when present.
# Values: regular | promo
PRICE_STATE=regular

# Auto-populated by Vercel when you connect an Edge Config store.
# EDGE_CONFIG=https://edge-config.vercel.com/ecfg_xxx?token=xxx
